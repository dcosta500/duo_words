name: duo_words
description: "App to help learning duolingo words."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  googleapis_auth: ^1.6.0
  http: ^1.2.2
  sprintf: ^7.0.0
  firebase_core: ^3.8.0
  cloud_firestore: ^5.5.0
  just_audio: ^0.9.42

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/